<% content_for :header_class, "header--is-opaque" %>

<%= form_for Shout.new do |f| %>
<div class="banner">
  <div class="banner__content">
    <img src="<%= current_user.profile_image_url %>" class="user__img user__img--is-large"/>
    <div class="user__flag-name"><%= link_to current_user.nickname, user_shouts_path(current_user) %></div>
  </div>
</div>
<%= render 'shout-tabs' %>

<%= form_for [current_user, Shout.new] do |f| %>


<div class="new-shout">
  <div class="container">
  <div class="new-shout__title">Give kudos to someone:</div>
  <%= f.text_area :message, placeholder: "Send a star to @twitteraccount", class: "new-shout__textarea" %>
  <div class="new-shout__button-wrapper">
    <%= f.submit 'Save', class: "button button--primary" %>
  </div>
  </div>
</div>
<% end %>
