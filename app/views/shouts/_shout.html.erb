<article class="shout">
  <div class="shout__text">
    <div class="shout__badge">
      ⭐️
    </div>
    <p>
      <%= shout.message %>
    </p>
  </div>
  <address class="shout__address">
    <b>@<%= shout.shouter.nickname %></b> sent on <%= shout.created_at.to_s(:short) %>
  </address>
  <% rand(4).downto(0) do %>
    <div class="reactions">
      <span class="reactions__emoji"><%= %w(😍 😘 😍 👏 🙌 ❤️).sample %></span>
      <span class="reactions__count"><%= rand(40) %></span>
    </div>
  <% end %>
  <div class="reactions reactions--button">
    <p class="emoji-picker-container">
      <%= text_field_tag :emoji, nil, class: 'add-reaction form-control', id: "add-reaction-shout-#{shout.id}", data: { emojiable: true, shout_id: shout.id } %>
    </p>
    Add reaction
  </div>
</article>
